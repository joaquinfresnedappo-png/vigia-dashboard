services:
  vigia:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - /home/coolify-admin/vigia:/usr/share/nginx/html:ro
    expose:
      - "80"
    networks:
      - coolify

networks:
  coolify:
    external: true
    name: coolify
